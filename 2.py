def largestMagical(binString):

    def helper(str):


def count(s):
    zeros = 0
    ones = 0
    for ch in s:
        if ch == "0":
            zeros += 1
        else:
            ones += 1

    return [zeros, ones]
